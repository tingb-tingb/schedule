# 排班助手 (Shift Scheduler)

一个简洁的排班管理应用，支持自定义班次和颜色标记。

## 功能

- 日历视图，直观查看每日排班
- 自定义班次类型（早班、中班、晚班、休息等）
- 每种班次可设置不同颜色
- 点击日期快速设置/切换班次
- 数据本地存储，无需联网

## 技术栈

- HTML + CSS + JavaScript（纯前端，无需后端）
- 使用 localStorage 持久化数据

## 使用方式

直接用浏览器打开 `index.html` 即可使用。
